import { TasksPipePipe } from './tasks-pipe.pipe';

describe('TasksPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TasksPipePipe();
    expect(pipe).toBeTruthy();
  });
});
